import {expect} from 'chai';
import {fetchAndParseTable, loadCache} from '../../src/actions';

// Trivial
describe('fetchAndParseTable', () => {
  it('should pass', () => {
    expect(true).to.eq(true);
  });
});